# splify Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Turn the `wg-split` powertoy into **splify** — a friendly, one-command-install OpenWrt product with a simple button-first UI, all power tucked under "Дополнительно", Russian docs, and a sing-box foundation.

**Architecture:** Same engine as upstream (shell core + nft/dnsmasq layer + procd failover daemon + least-privilege `rpcd` ubus object), rebranded `wg-split`→`splify`. The LuCI app gains a new simple **Главная** view that drives the engine through the existing ubus `action`/`status` methods (plus two new allow-list actions `on`/`off`); the full settings form becomes **Дополнительно**; the rich live dashboard is reused from a shared module behind a collapsible block on Главная. A `wget … | sh` installer pulls prebuilt `.apk` from GitHub Releases built by the existing OpenWrt-SDK CI.

**Tech Stack:** POSIX/ash shell, nftables, dnsmasq, OpenWrt UCI/LuCI (JS client-side views, `rpcd` jshn), GitHub Actions + OpenWrt SDK (Docker buildx), shellcheck + bats.

## Global Constraints

- Target platform: **OpenWrt 24.10+ / 25.12+ with the `apk` package manager** only.
- Product/brand name everywhere user-visible: **splify** (lowercase).
- Package names: `splify`, `luci-app-splify`, `luci-i18n-splify-ru`.
- Config file `/etc/config/splify`; ubus object `splify`; rpcd plugin `/usr/libexec/rpcd/splify`; nft drop-in `/etc/nftables.d/30-splify.nft`; lib dir `/usr/local/lib/splify/`.
- CLI commands prefixed `splify-` (e.g. `splify-doctor`, `splify-apply`).
- nft identifiers use the `splify_` prefix (e.g. `splify_ipsum_v4`, `splify_endpoint_mark_prerouting`, `splify_ipsum_mark_prerouting`).
- Env vars use the `SPLIFY_` prefix (e.g. `SPLIFY_WAIT_LOCK`).
- All user-facing UI text and all documentation: **Russian**. English strings may remain as the gettext source ids in code (translated via the `ru` `.po`).
- Repository: `github.com/xyzmean/splify`. Upstream list-source URLs (Re-filter-lists, radb-tools) are kept unchanged.
- Do **not** remove engine functionality (failover, lists, zapret, killswitch, doctor, firewall-fix, dashboard). Simplify presentation only.
- Every shell script must pass `shellcheck`; the bats suite must pass.
- VERSION starts at `1.0.0` for splify.

**Repo note:** all work happens in the local working tree `/root/splify` (seeded from upstream, `origin` removed). Use `git -C /root/splify …` if your shell cwd is elsewhere.

---

### Task 1: Sweeping rebrand `wg-split` → `splify`

Mechanical rename of every identifier, path, and filename. After this task the tree still describes the same engine, just renamed, and shellcheck stays clean.

**Files:**
- Rename (git mv): every path containing `wg-split` →`splify`:
  - `wg-split/` → `splify/` (package dir)
  - `luci-app-wg-split/` → `luci-app-splify/`
  - `splify/files/usr/local/lib/wg-split/` → `splify/files/usr/local/lib/splify/`
  - `splify/files/usr/local/sbin/wg-split-*` → `splify/files/usr/local/sbin/splify-*`
  - `splify/files/usr/libexec/rpcd/wg-split` → `…/rpcd/splify`
  - `splify/files/etc/config/wg-split` → `…/config/splify`
  - `splify/files/etc/init.d/wg-split` → `…/init.d/splify`
  - `splify/files/etc/nftables.d/30-wg-split.nft` → `30-splify.nft`
  - `splify/files/etc/uci-defaults/99-wg-split-migrate` → `99-splify-defaults`
  - `luci-app-splify/htdocs/luci-static/resources/view/wg-split/` → `…/view/splify/`
  - `luci-app-splify/po/ru/wg-split.po` → `…/po/ru/splify.po`
  - `luci-app-splify/root/usr/share/luci/menu.d/luci-app-wg-split.json` → `luci-app-splify.json`
  - `luci-app-splify/root/usr/share/rpcd/acl.d/luci-app-wg-split.json` → `luci-app-splify.json`
  - `tests/wg-split.bats` → `tests/splify.bats`
- Modify (content): all of the above plus `Dockerfile-apk`, `.github/workflows/*.yml`, `.shellcheckrc`, `tests/helper.bash`, both `Makefile`s.

**Interfaces:**
- Produces: the renamed identifiers listed in Global Constraints. Later tasks rely on: command paths `/usr/local/sbin/splify-*`, ubus object `splify`, config `splify`, lib `/usr/local/lib/splify/common.sh`, nft file `30-splify.nft`.

- [ ] **Step 1: Rename files and directories**

```bash
cd /root/splify
# package + app dirs
git mv wg-split splify
git mv luci-app-wg-split luci-app-splify
# inside splify package
git mv splify/files/usr/local/lib/wg-split splify/files/usr/local/lib/splify
for f in splify/files/usr/local/sbin/wg-split-*; do git mv "$f" "${f/wg-split-/splify-}"; done
git mv splify/files/usr/libexec/rpcd/wg-split splify/files/usr/libexec/rpcd/splify
git mv splify/files/etc/config/wg-split splify/files/etc/config/splify
git mv splify/files/etc/init.d/wg-split splify/files/etc/init.d/splify
git mv splify/files/etc/nftables.d/30-wg-split.nft splify/files/etc/nftables.d/30-splify.nft
git mv splify/files/etc/uci-defaults/99-wg-split-migrate splify/files/etc/uci-defaults/99-splify-defaults
# luci app internals
git mv luci-app-splify/htdocs/luci-static/resources/view/wg-split luci-app-splify/htdocs/luci-static/resources/view/splify
git mv luci-app-splify/po/ru/wg-split.po luci-app-splify/po/ru/splify.po
git mv luci-app-splify/root/usr/share/luci/menu.d/luci-app-wg-split.json luci-app-splify/root/usr/share/luci/menu.d/luci-app-splify.json
git mv luci-app-splify/root/usr/share/rpcd/acl.d/luci-app-wg-split.json luci-app-splify/root/usr/share/rpcd/acl.d/luci-app-splify.json
git mv tests/wg-split.bats tests/splify.bats
```

- [ ] **Step 2: Replace identifiers in file contents (order matters)**

Replace in this order so the most specific tokens go first. Operate on all tracked text files except the design/plan docs under `docs/superpowers/`.

```bash
cd /root/splify
FILES=$(git ls-files | grep -vE '^docs/superpowers/' | grep -vE '\.po$')
# 1) env var prefix  2) nft/identifier underscore form  3) docker feed name
sed -i 's/WG_SPLIT/SPLIFY/g'  $FILES
sed -i 's/wg_split/splify/g'  $FILES
sed -i 's/wgsplit/splify/g'   $FILES
# 4) repo slug, then the generic hyphen form
sed -i 's#xyzmean/m9-split-router#xyzmean/splify#g' $FILES
sed -i 's/m9-split-router/splify/g' $FILES
sed -i 's/wg-split/splify/g'   $FILES
# the .po needs the same identifier fixes but keep msgid English text intact:
sed -i 's/wg-split/splify/g; s/wg_split/splify/g' luci-app-splify/po/ru/splify.po
```

- [ ] **Step 3: Fix the LuCI view `require`/menu/ACL paths the sed missed**

The view module paths and rpc object names are now `splify`. Verify the menu JSON view paths read `splify/status` and `splify/settings`, the ACL key is `luci-app-splify` and grants ubus object `splify`, and the rpc `object:` in `status.js`/`settings.js` is `splify`. Grep to confirm none remain:

```bash
cd /root/splify
grep -rIn -e 'wg-split' -e 'wg_split' -e 'wgsplit' -e 'WG_SPLIT' --exclude-dir=.git --exclude-dir=docs . || echo "CLEAN"
```
Expected: `CLEAN` (no matches outside `docs/`).

- [ ] **Step 4: Run shellcheck on all shell scripts**

```bash
cd /root/splify
shellcheck splify/files/usr/local/sbin/splify-* \
  splify/files/usr/local/lib/splify/common.sh \
  splify/files/usr/libexec/rpcd/splify \
  splify/files/etc/init.d/splify
```
Expected: no errors (warnings already suppressed via `.shellcheckrc`/inline directives). Fix any path the rename broke.

- [ ] **Step 5: Sanity-check the bats suite references**

`tests/splify.bats` and `tests/helper.bash` must point at the new `splify-*` script paths and `splify` config. If `bats` is installed, run `bats tests/splify.bats`; otherwise grep for stale paths:
```bash
cd /root/splify && grep -nE 'wg-split|wg_split' tests/* || echo "tests clean"
```
Expected: `tests clean` (and, if bats present, the suite passes).

- [ ] **Step 6: Commit**

```bash
cd /root/splify
git add -A
git commit -m "refactor: rebrand wg-split -> splify across tree"
```

---

### Task 2: Product metadata & history reset

Make the repo read as a fresh product, not a fork mid-stream.

**Files:**
- Modify: `VERSION`, `splify/Makefile`, `luci-app-splify/Makefile`
- Replace: `CHANGELOG.md`
- Delete: `roadmap.md`, `docs/superpowers/specs/2026-06-24-wg-split-2.0.0-design.md` (upstream's, not ours)

**Interfaces:**
- Consumes: renamed tree from Task 1.
- Produces: `VERSION` = `1.0.0`; Makefile `TITLE`/`URL`/`description` describing splify.

- [ ] **Step 1: Set version to 1.0.0**

```bash
cd /root/splify && printf '1.0.0\n' > VERSION
```

- [ ] **Step 2: Update package Makefile metadata**

In `splify/Makefile` set:
```
TITLE:=splify — простой split-tunnel VPN-шлюз для OpenWrt
URL:=https://github.com/xyzmean/splify
```
and replace the `Package/splify/description` block body with a short Russian description:
```
 splify маршрутизирует трафик локальной сети через WireGuard/AmneziaWG-туннель:
 заблокированное идёт через VPN, остальное — напрямую. Приоритетный failover между
 туннелями, автообновляемые списки, опциональный обход DPI (zapret). Управление —
 через простую страницу в LuCI. zapret опционален и определяется в рантайме.
```
In `luci-app-splify/Makefile` set `LUCI_TITLE:=splify — веб-интерфейс` and confirm `LUCI_DEPENDS:=+luci-base +splify`.

- [ ] **Step 3: Reset CHANGELOG**

Overwrite `CHANGELOG.md`:
```markdown
# История изменений

## 1.0.0
- Первый релиз splify: установка одной командой, простой интерфейс,
  расширенные настройки под вкладкой «Дополнительно», документация на русском.
- Заложен фундамент под sing-box (пока без рабочего бэкенда).
```

- [ ] **Step 4: Remove upstream-only docs**

```bash
cd /root/splify
git rm roadmap.md "docs/superpowers/specs/2026-06-24-wg-split-2.0.0-design.md"
```

- [ ] **Step 5: Commit**

```bash
cd /root/splify
git add -A
git commit -m "chore: splify product metadata, version 1.0.0, reset changelog"
```

---

### Task 3: rpcd `on`/`off` actions for the simple toggle

The Главная toggle needs a clean "turn routing on / off" that sticks. Add two allow-listed actions to the rpcd object. No engine logic changes — just service control wrappers.

**Files:**
- Modify: `splify/files/usr/libexec/rpcd/splify` (action case)
- Modify: `tests/splify.bats` (add a test asserting the two actions are recognized)

**Interfaces:**
- Consumes: ubus object `splify`, method `action` with params `{name, iface}` (existing).
- Produces: actions `on` and `off` callable via `callAction('on','')` / `callAction('off','')`, returning `{code:int, stdout:string}` like the others. `on` = enable+restart the service (engine applies on start). `off` = stop+disable the service then `splify-disable` (drop to WAN).

- [ ] **Step 1: Add the `on`/`off` branches**

In the `action` `case "$name"` block of `splify/files/usr/libexec/rpcd/splify`, add before the `*)` default:
```sh
                on)
                    /etc/init.d/splify enable >/dev/null 2>&1
                    out="$(/etc/init.d/splify restart 2>&1)"; code=$? ;;
                off)
                    /etc/init.d/splify disable >/dev/null 2>&1
                    /etc/init.d/splify stop >/dev/null 2>&1
                    out="$(/usr/local/sbin/splify-disable 2>&1)"; code=$? ;;
```

- [ ] **Step 2: Allow the new actions in the bats suite**

Add to `tests/splify.bats`:
```bash
@test "rpcd action handles on/off" {
  run grep -nE '^\s*(on|off)\)' splify/files/usr/libexec/rpcd/splify
  [ "$status" -eq 0 ]
}
```
(If the suite's path helpers differ, mirror the style already used in `tests/splify.bats`.)

- [ ] **Step 3: shellcheck the rpcd plugin**

```bash
cd /root/splify && shellcheck splify/files/usr/libexec/rpcd/splify
```
Expected: no errors.

- [ ] **Step 4: Commit**

```bash
cd /root/splify
git add splify/files/usr/libexec/rpcd/splify tests/splify.bats
git commit -m "feat(rpcd): add on/off service actions for the simple toggle"
```

---

### Task 4: Reuse the dashboard as a shared module

Extract the live dashboard builder from `status.js` into a require-able module so the new Главная view can embed it under a collapsible block (DRY — don't duplicate ~480 lines).

**Files:**
- Create: `luci-app-splify/htdocs/luci-static/resources/splify/dashboard.js`
- Modify: `luci-app-splify/htdocs/luci-static/resources/view/splify/status.js`

**Interfaces:**
- Produces: a LuCI class module `splify.dashboard` exporting `buildPanel(wgIfaces)` → returns a DOM node (the full hero + chain + tables + diagnostics + timeline + toolbar, with its own polling), and `injectStyle()`. Consumed by Task 5's `home.js` and by `status.js`.

- [ ] **Step 1: Create the shared module**

Create `resources/splify/dashboard.js` as an `L.Class` (LuCI's `baseclass`) wrapping the existing logic from `status.js`. Move the helper functions and `statusPanel()` body into it verbatim (they already exist in `status.js` — relocate, don't rewrite), exposing:
```js
'use strict';
'require baseclass';
'require rpc';
'require ui';
'require poll';

var callStatus = rpc.declare({ object: 'splify', method: 'status' });
var callEvents = rpc.declare({ object: 'splify', method: 'events' });
var callAction = rpc.declare({ object: 'splify', method: 'action', params: [ 'name', 'iface' ] });

// … (move SEV, injectStyle, badge, yn, healthCell, fmtAge, fmtRate, prevTx,
//     ratesFor, rateLabel, pathInfo, heroCard, chainViz, checkLink, statusPanel
//     here UNCHANGED from status.js) …

return baseclass.extend({
    injectStyle: injectStyle,
    // statusPanel(wgIfaces) already returns the full panel node
    buildPanel: function (wgIfaces) { return statusPanel(wgIfaces); }
});
```

- [ ] **Step 2: Slim `status.js` to use the module**

Replace the body of `status.js` so it just renders the shared panel (keeps the existing menu route working):
```js
'use strict';
'require view';
'require network';
'require splify.dashboard as dashboard';

return view.extend({
    load: function () { return network.getNetworks(); },
    render: function (networks) {
        var wgIfaces = (networks || []).filter(function (n) {
            var p = n.getProtocol();
            return p === 'amneziawg' || p === 'wireguard';
        }).map(function (n) { return n.getName(); });
        return dashboard.buildPanel(wgIfaces);
    },
    handleSave: null, handleSaveApply: null, handleReset: null
});
```

- [ ] **Step 3: Verify no duplicate symbols / syntax**

```bash
cd /root/splify && node --check luci-app-splify/htdocs/luci-static/resources/splify/dashboard.js \
  && node --check luci-app-splify/htdocs/luci-static/resources/view/splify/status.js \
  && echo "JS OK"
```
Expected: `JS OK`. (LuCI's `'require'` comments are plain comments to Node, so `--check` validates syntax.)

- [ ] **Step 4: Commit**

```bash
cd /root/splify
git add luci-app-splify/htdocs/luci-static/resources/splify/dashboard.js \
        luci-app-splify/htdocs/luci-static/resources/view/splify/status.js
git commit -m "refactor(luci): extract dashboard into shared splify.dashboard module"
```

---

### Task 5: New simple "Главная" view + menu restructure

The product's front door: a big On/Off toggle, a "connect through" tunnel picker, a plain-language status line, list-refresh and firewall-fix buttons, and a collapsible "Подробное состояние" embedding the dashboard.

**Files:**
- Create: `luci-app-splify/htdocs/luci-static/resources/view/splify/home.js`
- Modify: `luci-app-splify/root/usr/share/luci/menu.d/luci-app-splify.json`
- Modify: `luci-app-splify/htdocs/luci-static/resources/view/splify/settings.js` (Russian labels + section header)

**Interfaces:**
- Consumes: `splify.dashboard` (`buildPanel`, `injectStyle`); ubus `splify` actions `on`/`off`/`update_ipsum`/`update_ru`/`update_domains`/`fw_fix`; `splify-doctor --json` shape (`overall`, `summary.state`, `endpoints[]`, `checks[]`); UCI `splify` (endpoint sections).
- Produces: menu entry `admin/services/splify` (firstchild) → `Главная` (order 1, default) + `Дополнительно` (order 2). Status route kept reachable but not a top menu item (see Step 2).

- [ ] **Step 1: Write the Главная view**

Create `resources/view/splify/home.js`:
```js
'use strict';
'require view';
'require network';
'require uci';
'require rpc';
'require ui';
'require poll';
'require splify.dashboard as dashboard';

var callStatus = rpc.declare({ object: 'splify', method: 'status' });
var callAction = rpc.declare({ object: 'splify', method: 'action', params: [ 'name', 'iface' ] });

// Plain-language status from doctor's summary.state.
function statusLine(d) {
    var s = (d && d.summary) || {}, st = s.state || '';
    if (/^vpn:/.test(st)) return { ok: true,  text: _('Защищено — трафик идёт через VPN (%s)').format(st.replace(/^vpn:/, '')) };
    if (st === 'zapret')  return { ok: false, text: _('Нет туннеля — работает обход DPI на прямом канале') };
    if (st === 'killswitch') return { ok: false, text: _('Защита включена, но туннель недоступен — трафик заблокирован') };
    if (st === 'wan')     return { ok: false, text: _('Прямое подключение — VPN не активен') };
    return { ok: false, text: _('Состояние неизвестно') };
}
function isOn(d) { var st = ((d && d.summary) || {}).state || ''; return /^vpn:/.test(st) || st === 'zapret' || st === 'killswitch'; }

// First firewall problem doctor can auto-fix (message starts "<iface>: …").
function firewallFix(d) {
    var c = (d && d.checks || []).filter(function (x) {
        return x.category === 'firewall' && x.severity !== 'OK' &&
               !/in the shared |device wildcard|non-tunnel networks/.test(x.message || '');
    })[0];
    if (!c) return null;
    var m = /^([A-Za-z0-9_.-]+):/.exec(c.message || '');
    return m ? m[1] : null;
}

return view.extend({
    load: function () {
        return Promise.all([ network.getNetworks(), uci.load('splify') ]);
    },
    render: function (data) {
        dashboard.injectStyle();
        var networks = data[0] || [];
        var wgIfaces = networks.filter(function (n) {
            var p = n.getProtocol(); return p === 'amneziawg' || p === 'wireguard';
        }).map(function (n) { return n.getName(); });

        var body = E('div', {}, E('em', {}, _('Загрузка…')));
        var detailsBox = E('div', { 'style': 'display:none;margin-top:1em' });
        var detailsBuilt = false;

        function busy(node, fn) {
            node.disabled = true;
            return Promise.resolve().then(fn).finally(function () { node.disabled = false; });
        }

        function setTunnel(iface) {
            // Write iface onto the priority-1 endpoint (create one if none).
            var secs = uci.sections('splify', 'endpoint');
            var sid = secs.length ? secs[0]['.name'] : uci.add('splify', 'endpoint');
            uci.set('splify', sid, 'iface', iface);
            if (!uci.get('splify', sid, 'priority')) uci.set('splify', sid, 'priority', '1');
            if (!uci.get('splify', sid, 'type')) uci.set('splify', sid, 'type', 'wg');
            return uci.save().then(function () { return callAction('apply', ''); });
        }

        function refresh() {
            return callStatus().then(function (d) {
                d = d || {};
                var sl = statusLine(d), on = isOn(d), fwIface = firewallFix(d);

                var toggle = E('button', {
                    'class': 'btn cbi-button ' + (on ? 'cbi-button-reset' : 'cbi-button-save'),
                    'style': 'font-size:1.1em;padding:.5em 1.4em',
                    'click': ui.createHandlerFn(this, function (ev) {
                        return busy(ev.target, function () {
                            return callAction(on ? 'off' : 'on', '').then(function () {
                                ui.addNotification(null, E('p', {}, on ? _('VPN выключен') : _('VPN включён')), 'info');
                                return refresh();
                            });
                        });
                    })
                }, on ? _('Выключить') : _('Включить'));

                var picker;
                if (wgIfaces.length) {
                    var cur = (uci.sections('splify', 'endpoint')[0] || {}).iface || '';
                    var sel = E('select', { 'class': 'cbi-input-select' },
                        wgIfaces.map(function (i) {
                            return E('option', Object.assign({ 'value': i }, i === cur ? { 'selected': 'selected' } : {}), i);
                        }));
                    sel.addEventListener('change', ui.createHandlerFn(this, function () {
                        return busy(sel, function () {
                            return setTunnel(sel.value).then(function () {
                                ui.addNotification(null, E('p', {}, _('Туннель переключён на %s').format(sel.value)), 'info');
                                return refresh();
                            });
                        });
                    }));
                    picker = E('div', { 'style': 'margin:.6em 0' }, [
                        E('label', { 'style': 'margin-right:.5em' }, _('Подключаться через:')), sel ]);
                } else {
                    picker = E('div', { 'class': 'alert-message warning', 'style': 'margin:.6em 0' }, [
                        _('Сначала создайте VPN-туннель в '),
                        E('a', { 'href': L.url('admin/network/network') }, _('Сеть → Интерфейсы')),
                        _(' (WireGuard или AmneziaWG).') ]);
                }

                var tools = [
                    E('button', { 'class': 'btn cbi-button cbi-button-neutral',
                        'click': ui.createHandlerFn(this, function (ev) {
                            return busy(ev.target, function () {
                                return Promise.all([ callAction('update_ipsum',''), callAction('update_ru',''), callAction('update_domains','') ])
                                    .then(function () { ui.addNotification(null, E('p', {}, _('Списки обновлены')), 'info'); return refresh(); });
                            });
                        }) }, _('Обновить списки'))
                ];
                if (fwIface) tools.push(E('button', { 'class': 'btn cbi-button cbi-button-action',
                    'click': ui.createHandlerFn(this, function (ev) {
                        return busy(ev.target, function () {
                            return callAction('fw_fix', fwIface).then(function (r) {
                                r = r || {};
                                ui.addNotification(null, E('p', {}, r.code === 0 ? _('Брандмауэр настроен') : _('Не удалось настроить брандмауэр: ') + (r.stdout||'')), r.code === 0 ? 'info' : 'warning');
                                return refresh();
                            });
                        });
                    }) }, _('Починить автоматически')));

                var detailsToggle = E('a', { 'href': '#', 'style': 'display:inline-block;margin-top:1em',
                    'click': function (e) {
                        e.preventDefault();
                        var open = detailsBox.style.display === 'none';
                        detailsBox.style.display = open ? 'block' : 'none';
                        if (open && !detailsBuilt) { detailsBuilt = true; detailsBox.appendChild(dashboard.buildPanel(wgIfaces)); }
                        e.target.textContent = open ? _('Скрыть подробное состояние') : _('Подробное состояние');
                    } }, _('Подробное состояние'));

                body.replaceChildren(E('div', { 'class': 'cbi-section' }, [
                    E('div', { 'class': 'wgs-hero', 'style': 'border-left:5px solid ' + (sl.ok ? '#3c763d' : '#a94442') }, [
                        E('div', { 'class': 'wgs-hero-icon', 'style': 'color:' + (sl.ok ? '#3c763d' : '#a94442') }, sl.ok ? '✓' : '!'),
                        E('div', { 'class': 'wgs-hero-main' }, [ E('div', { 'class': 'wgs-hero-path' }, sl.text) ]),
                        E('div', { 'style': 'flex:0 0 auto' }, toggle)
                    ]),
                    picker,
                    E('div', { 'class': 'wgs-toolbar' }, tools),
                    detailsToggle
                ]));
            }).catch(function (e) {
                body.replaceChildren(E('em', { 'style': 'color:#a94442' }, _('Не удалось получить состояние (служба splify запущена?): ') + e));
            });
        }

        refresh();
        poll.add(refresh, 8);
        return E('div', {}, [ E('h2', {}, 'splify'), body, detailsBox ]);
    },
    handleSave: null, handleSaveApply: null, handleReset: null
});
```

- [ ] **Step 2: Restructure the menu**

Overwrite `luci-app-splify/root/usr/share/luci/menu.d/luci-app-splify.json`:
```json
{
	"admin/services/splify": {
		"title": "splify",
		"order": 60,
		"action": { "type": "firstchild" },
		"depends": { "acl": [ "luci-app-splify" ] }
	},
	"admin/services/splify/home": {
		"title": "Главная",
		"order": 1,
		"action": { "type": "view", "path": "splify/home" },
		"depends": { "acl": [ "luci-app-splify" ] }
	},
	"admin/services/splify/settings": {
		"title": "Дополнительно",
		"order": 2,
		"action": { "type": "view", "path": "splify/settings" },
		"depends": { "acl": [ "luci-app-splify" ] }
	}
}
```
(The `splify/status` view file stays in the build for the embedded dashboard module; it is simply no longer its own menu item — the dashboard is reached via "Подробное состояние" on Главная.)

- [ ] **Step 3: Russianize the settings form**

In `settings.js`, translate the user-facing strings to Russian and rename the page title shown in the form to `Дополнительные настройки`. Examples (apply to every `_()` label/description in the file):
- `_('Routing mode')` → `_('Режим маршрутизации')`
- mode option captions → `_('Через VPN только заблокированное (рекомендуется)')`, `_('Всё через VPN, исключения — напрямую')`, `_('Через VPN только выбранные списки/домены')`
- `_('Failover interval')` → `_('Интервал проверки (сек)')`
- `_('Kill switch')` → `_('Блокировать трафик, если туннель недоступен')`
- `_('Failover tunnels')` → `_('Туннели (с приоритетом)')`
- `_('Per-device overrides')` → `_('Правила для отдельных устройств')`
- lists/manual labels similarly. Keep field names/datatypes unchanged.

- [ ] **Step 4: Syntax-check the JS and JSON**

```bash
cd /root/splify
node --check luci-app-splify/htdocs/luci-static/resources/view/splify/home.js
node --check luci-app-splify/htdocs/luci-static/resources/view/splify/settings.js
python3 -m json.tool luci-app-splify/root/usr/share/luci/menu.d/luci-app-splify.json >/dev/null && echo "JSON OK"
```
Expected: no errors; `JSON OK`.

- [ ] **Step 5: Commit**

```bash
cd /root/splify
git add luci-app-splify
git commit -m "feat(luci): simple Главная view + menu restructure, Дополнительно form in Russian"
```

---

### Task 6: Russian UI translation (`.po`)

Provide `ru` translations for every new/changed `_()` string so the UI is Russian when `luci-i18n-splify-ru` is installed (English source ids stay as fallback).

**Files:**
- Modify: `luci-app-splify/po/ru/splify.po`

**Interfaces:**
- Consumes: the English `_()` msgids present in `home.js`, `dashboard.js`, `settings.js`.

- [ ] **Step 1: Collect msgids**

```bash
cd /root/splify
grep -rhoE "_\('([^']|\\\\')*'\)" luci-app-splify/htdocs/luci-static/resources | sort -u | head -200
```
Use this list to ensure every English msgid has a `ru` `msgstr`.

- [ ] **Step 2: Add/extend translations**

For each English msgid used in the views, add a `msgid`/`msgstr` pair to `splify.po` (Russian). For the strings already authored in Russian in `home.js`, the msgid is the Russian text itself — add identity entries or leave them (gettext falls back to the id). Ensure at minimum the `dashboard.js` (ex-`status.js`) English strings (`Status & diagnostics`, `Failover tunnels`, `Lists`, `Diagnostics`, `Recent failover events`, `Refresh now`, `Apply`, `Restart service`, `Emergency disable`, `VPN active via %s`, `No tunnel — direct WAN (traffic exposed)`, etc.) all have Russian `msgstr`.

- [ ] **Step 3: Validate the .po**

```bash
cd /root/splify && msgfmt -c luci-app-splify/po/ru/splify.po -o /dev/null && echo "PO OK"
```
Expected: `PO OK`. (If `msgfmt` is unavailable, visually verify balanced `msgid`/`msgstr` quotes.)

- [ ] **Step 4: Commit**

```bash
cd /root/splify
git add luci-app-splify/po/ru/splify.po
git commit -m "i18n: Russian translations for splify UI"
```

---

### Task 7: One-command installer & uninstaller

`wget … | sh` that pulls the latest release `.apk` and installs them, plus a symmetric uninstaller.

**Files:**
- Create: `install.sh`
- Create: `uninstall.sh`

**Interfaces:**
- Consumes: GitHub Releases of `xyzmean/splify` containing `splify-*.apk`, `luci-app-splify-*.apk`, `luci-i18n-splify-ru-*.apk`.
- Produces: `install.sh` usable as `wget -O - https://raw.githubusercontent.com/xyzmean/splify/main/install.sh | sh`.

- [ ] **Step 1: Write `install.sh`**

```sh
#!/bin/sh
# splify installer — качает последний релиз и ставит пакеты.
# Использование на роутере OpenWrt:
#   wget -O - https://raw.githubusercontent.com/xyzmean/splify/main/install.sh | sh
set -eu

REPO="xyzmean/splify"
API="https://api.github.com/repos/$REPO/releases/latest"
TMP="$(mktemp -d /tmp/splify.XXXXXX)"
trap 'rm -rf "$TMP"' EXIT

say()  { printf '\033[1;36m==>\033[0m %s\n' "$*"; }
err()  { printf '\033[1;31mОшибка:\033[0m %s\n' "$*" >&2; exit 1; }

# 1) проверки окружения
[ "$(id -u)" = "0" ] || err "запустите от root."
command -v apk  >/dev/null 2>&1 || err "нужен OpenWrt 24.10+/25.12+ с менеджером apk."
command -v wget >/dev/null 2>&1 || err "не найден wget."

# 2) узнать ссылки на .apk из последнего релиза
say "Ищу последний релиз splify…"
META="$TMP/meta.json"
wget -qO "$META" "$API" || err "не удалось получить данные релиза (нет интернета?)."
URLS="$(sed -n 's/.*"browser_download_url": *"\([^"]*\.apk\)".*/\1/p' "$META")"
[ -n "$URLS" ] || err "в последнем релизе нет .apk. Возможно, релиз ещё не собран."

# 3) скачать пакеты
say "Скачиваю пакеты…"
for u in $URLS; do
  case "$u" in
    *splify*) wget -qP "$TMP" "$u" || err "не удалось скачать $u" ;;
  esac
done
ls "$TMP"/*.apk >/dev/null 2>&1 || err "пакеты не скачались."

# 4) установить (зависимости подтянутся из фидов)
say "Устанавливаю…"
apk add --allow-untrusted "$TMP"/*.apk || err "apk add не выполнился."

say "Готово! Дальше:"
printf '  1. Создайте VPN-туннель: Сеть → Интерфейсы (WireGuard/AmneziaWG).\n'
printf '  2. Откройте Сервисы → splify → Главная и нажмите «Включить».\n'
```

- [ ] **Step 2: Write `uninstall.sh`**

```sh
#!/bin/sh
# splify — удаление.
#   wget -O - https://raw.githubusercontent.com/xyzmean/splify/main/uninstall.sh | sh
set -eu
[ "$(id -u)" = "0" ] || { echo "запустите от root." >&2; exit 1; }
command -v apk >/dev/null 2>&1 || { echo "apk не найден." >&2; exit 1; }
echo "==> Удаляю splify…"
apk del luci-i18n-splify-ru luci-app-splify splify 2>/dev/null || apk del luci-app-splify splify
echo "Готово. Конфигурация /etc/config/splify сохранена (удалите вручную при желании)."
```

- [ ] **Step 3: Make executable & shellcheck**

```bash
cd /root/splify && chmod +x install.sh uninstall.sh && shellcheck install.sh uninstall.sh
```
Expected: no errors.

- [ ] **Step 4: Dry-run the release-parsing logic**

Verify the `sed` URL extraction works against a sample GitHub release JSON:
```bash
cd /root/splify
printf '%s\n' '{"assets":[{"browser_download_url":"https://x/splify-1.0.0.apk"},{"browser_download_url":"https://x/luci-app-splify-1.0.0.apk"}]}' \
  | sed -n 's/.*"browser_download_url": *"\([^"]*\.apk\)".*/\1/p'
```
Expected: the two `.apk` URLs printed, one per line.

- [ ] **Step 5: Commit**

```bash
cd /root/splify
git add install.sh uninstall.sh
git commit -m "feat: one-command install.sh / uninstall.sh"
```

---

### Task 8: sing-box foundation

Formalize the transport seam so a future sing-box backend drops in: keep `type=wg` default, give the dispatcher a documented `singbox` stub, expose `sing-box (скоро)` in the advanced form, and document it.

**Files:**
- Modify: `splify/files/usr/local/lib/splify/common.sh` (the `ep_*` dispatch helpers)
- Modify: `splify/files/etc/config/splify` (comment the `type` field)
- Modify: `luci-app-splify/htdocs/luci-static/resources/view/splify/settings.js` (type option)
- Create: `docs/ru/sing-box.md`

**Interfaces:**
- Consumes: existing `ep_*` dispatch helpers keyed on endpoint `type` in `common.sh`.
- Produces: `type` accepts `wg` (default, implemented) and `singbox` (stub that logs "not implemented" and is treated as down, never crashing the failover loop).

- [ ] **Step 1: Add the `singbox` stub branch**

In `common.sh`, find each `ep_*` dispatcher's `case "$type"` (or equivalent) and add a `singbox)` branch that logs once and returns a non-healthy/no-op result, e.g.:
```sh
        singbox)
            logger -t splify "sing-box backend not implemented yet (endpoint $iface); skipping"
            return 1 ;;
```
Match the return convention each helper already uses for an unavailable endpoint so the failover loop simply treats a `singbox` endpoint as down.

- [ ] **Step 2: Document the type field in UCI defaults**

In `splify/files/etc/config/splify`, near the endpoint section, ensure a documented `type`:
```
	# type: транспорт туннеля. wg = WireGuard/AmneziaWG (работает).
	#       singbox = sing-box (VLESS/Hysteria) — пока заглушка, в планах.
	option type 'wg'
```

- [ ] **Step 3: Expose the option in the advanced form**

In `settings.js` where the endpoint `type` `ListValue` is built, add the second choice (kept `modalonly`):
```js
o.value('wg', _('WireGuard / AmneziaWG'));
o.value('singbox', _('sing-box (скоро)'));
o.default = 'wg';
```

- [ ] **Step 4: Write the docs page**

Create `docs/ru/sing-box.md`:
```markdown
# sing-box (в планах)

splify построен так, чтобы кроме WireGuard/AmneziaWG поддержать и **sing-box**
(VLESS, Hysteria и т.п.). Сейчас это **фундамент**: у туннеля есть поле «тип»
(`wg` или `singbox`), а движок умеет различать типы — но рабочего sing-box-бэкенда
пока нет. Если выбрать тип `sing-box`, туннель считается недоступным.

Когда бэкенд появится, его можно будет включить здесь же, в разделе
«Дополнительно», не меняя остальную настройку.
```

- [ ] **Step 5: shellcheck + JS check**

```bash
cd /root/splify
shellcheck splify/files/usr/local/lib/splify/common.sh
node --check luci-app-splify/htdocs/luci-static/resources/view/splify/settings.js && echo OK
```
Expected: no errors; `OK`.

- [ ] **Step 6: Commit**

```bash
cd /root/splify
git add splify/files/usr/local/lib/splify/common.sh splify/files/etc/config/splify \
        luci-app-splify/htdocs/luci-static/resources/view/splify/settings.js docs/ru/sing-box.md
git commit -m "feat: sing-box foundation (type seam stub, UI option, docs)"
```

---

### Task 9: Russian documentation

Rewrite the docs for an ordinary user: plain language, no nft/ip-rule jargon on the main path; one short "для продвинутых" appendix.

**Files:**
- Replace: `README.md`
- Replace: `docs/ru/Home.md`, `docs/ru/Installation.md`
- Create: `docs/ru/Быстрый-старт.md`, `docs/ru/Как-это-работает.md`, `docs/ru/Простой-режим.md`, `docs/ru/Дополнительно.md`, `docs/ru/Решение-проблем.md`, `docs/ru/Для-продвинутых.md`
- Russianize/simplify: `docs/ru/FAQ.md`
- Remove (condensed into `Для-продвинутых.md` / `Решение-проблем.md`): `docs/ru/CLI-Reference.md`, `docs/ru/Configuration.md`, `docs/ru/Diagnostics.md`, `docs/ru/Failover.md`, `docs/ru/Lists-and-zapret.md`, `docs/ru/Routing-Modes.md`, `docs/ru/Troubleshooting.md`

**Interfaces:** none (documentation).

- [ ] **Step 1: Write `README.md` (Russian product front page)**

```markdown
# splify

**splify** превращает роутер с OpenWrt в умный VPN-шлюз: заблокированные сайты
идут через VPN, всё остальное — напрямую и на полной скорости. Настройка — одна
команда и пара кнопок.

## Установка одной командой

На роутере (OpenWrt 24.10+/25.12+) выполните:

    wget -O - https://raw.githubusercontent.com/xyzmean/splify/main/install.sh | sh

## Быстрый старт

1. Создайте VPN-туннель: **Сеть → Интерфейсы** (WireGuard или AmneziaWG).
2. Откройте **Сервисы → splify → Главная**, выберите туннель и нажмите **«Включить»**.

Готово. Подробнее — в [документации](docs/ru/Home.md).

## Что умеет

- Простой режим: одна кнопка вкл/выкл и выбор туннеля.
- Автоматические списки заблокированного — обновляются сами.
- Резервные туннели с переключением при сбое.
- Обход блокировок по DPI (zapret), если он установлен.
- Всё тонкое — под вкладкой **«Дополнительно»**.
- Заложен фундамент под **sing-box** (см. [docs/ru/sing-box.md](docs/ru/sing-box.md)).

## Удаление

    wget -O - https://raw.githubusercontent.com/xyzmean/splify/main/uninstall.sh | sh
```

- [ ] **Step 2: Write `docs/ru/Home.md`** — short index linking the pages: Установка, Быстрый старт, Как это работает, Простой режим, Дополнительно, Решение проблем, FAQ, sing-box, Для продвинутых. Use exact filenames so links resolve.

- [ ] **Step 3: Rewrite `docs/ru/Installation.md`** — the one-command install front and center; requirements (OpenWrt 24.10+/apk); what the installer does (ставит пакеты, поднимает службу и ежедневное обновление списков); manual alternative (`apk add ./*.apk` from a release); removal.

- [ ] **Step 4: Write `docs/ru/Быстрый-старт.md`** — create tunnel → open Главная → pick tunnel → Включить; what each status line means; the «Обновить списки» / «Починить автоматически» buttons.

- [ ] **Step 5: Write `docs/ru/Как-это-работает.md`** — one simple diagram (LAN → VPN для заблокированного / напрямую для остального), no nft/ip-rule terminology.

- [ ] **Step 6: Write `docs/ru/Простой-режим.md` and `docs/ru/Дополнительно.md`** — describe the Главная controls; and an overview of the advanced settings (режимы, туннели/приоритеты, списки, zapret, killswitch, ручные подсети/домены, правила устройств) in plain terms.

- [ ] **Step 7: Write `docs/ru/Решение-проблем.md`** — top user problems and fixes in plain Russian (нет интернета после включения → проверьте брандмауэр кнопкой «Починить автоматически»; туннель не поднимается → проверьте ключи/endpoint; списки пустые → «Обновить списки»). Fold the old Troubleshooting table essentials here.

- [ ] **Step 8: Write `docs/ru/Для-продвинутых.md`** — condense CLI (`splify-doctor`, `splify-apply`, `splify-status`, `logread -e splify`), config file location, routing modes detail, lists/zapret detail. Then remove the now-redundant pages:

```bash
cd /root/splify
git rm docs/ru/CLI-Reference.md docs/ru/Configuration.md docs/ru/Diagnostics.md \
       docs/ru/Failover.md docs/ru/Lists-and-zapret.md docs/ru/Routing-Modes.md \
       docs/ru/Troubleshooting.md
```

- [ ] **Step 9: Verify links resolve**

```bash
cd /root/splify
missing=0
for f in $(grep -rhoE '\(([^)]+\.md)\)' docs/ru README.md | tr -d '()' | sed 's#.*/##' | sort -u); do
  if ! find docs/ru -name "$f" | grep -q . && [ "$f" != "README.md" ]; then echo "MISSING: $f"; missing=1; fi
done
[ "$missing" = 0 ] && echo "links OK"
```
Expected: `links OK`, no `MISSING:` lines.

- [ ] **Step 10: Commit**

```bash
cd /root/splify
git add -A docs README.md
git commit -m "docs: rewrite documentation in plain Russian for end users"
```

---

### Task 10: CI rebrand, repo publish, release & install verification

Build the `.apk`, publish the first release, and prove the one-command install works end to end.

**Files:**
- Verify (already sed-renamed in Task 1): `.github/workflows/build.yml`, `release.yml`, `test.yml`, `auto-version.yml`, `Dockerfile-apk`.

**Interfaces:**
- Consumes: the whole renamed tree; `gh` CLI authenticated as `xyzmean`.
- Produces: public repo `xyzmean/splify` with a `v1.0.0` release carrying the three `.apk` + `install.sh`.

- [ ] **Step 1: Confirm CI references are renamed**

```bash
cd /root/splify
grep -rInE 'wg-split|wg_split|wgsplit|m9-split-router' .github Dockerfile-apk || echo "CI CLEAN"
```
Expected: `CI CLEAN`. Confirm `Dockerfile-apk` uses feed name `splify` (`src-link splify …`) and builds `package/splify/compile package/luci-app-splify/compile`; confirm `release.yml` `files:` glob matches `splify*.apk`/`luci-*splify*.apk` and its install snippet shows the `wget | sh` command.

- [ ] **Step 2: Attach `install.sh` to releases**

In `release.yml`, add `install.sh` to the `softprops/action-gh-release` `files:` list so it is downloadable as a release asset too.

- [ ] **Step 3: Commit CI tweaks**

```bash
cd /root/splify
git add .github
git commit -m "ci: splify release attaches install.sh; rebrand verified" || echo "nothing to commit"
```

- [ ] **Step 4: Create the GitHub repo and push**

```bash
cd /root/splify
gh repo create xyzmean/splify --public --source=. --remote=origin \
  --description "Простой split-tunnel VPN-шлюз для OpenWrt — установка одной командой" --push
```
Expected: repo created, `main` pushed.

- [ ] **Step 5: Run the release workflow**

```bash
cd /root/splify
gh workflow run release.yml -f version=1.0.0
sleep 5; gh run list --workflow=release.yml -L 1
```
Then watch it: `gh run watch <run-id> --exit-status`. Expected: success. If the OpenWrt SDK build fails, read the log (`gh run view <run-id> --log-failed`), fix the Makefile/Dockerfile issue, commit, push, and re-run (budget 1–2 iterations).

- [ ] **Step 6: Verify the release assets**

```bash
gh release view v1.0.0 --json assets --jq '.assets[].name'
```
Expected: `splify-1.0.0*.apk`, `luci-app-splify-1.0.0*.apk`, `luci-i18n-splify-ru-1.0.0*.apk`, `install.sh`.

- [ ] **Step 7: Verify the installer resolves real release URLs**

```bash
wget -qO- https://api.github.com/repos/xyzmean/splify/releases/latest \
  | sed -n 's/.*"browser_download_url": *"\([^"]*\.apk\)".*/\1/p'
```
Expected: the three `.apk` download URLs. (Full on-router `apk add` requires an OpenWrt device/VM; this confirms the installer's discovery step against the live release.)

- [ ] **Step 8: Final state**

Confirm `wget -O - https://raw.githubusercontent.com/xyzmean/splify/main/install.sh | sh` is the documented, working entry point and the release page shows it. Done.

---

## Self-Review

**Spec coverage:**
- One-command install → Tasks 7, 10. ✅
- Simple button UI default + Advanced tab → Tasks 4, 5. ✅
- Keep all engine functionality → preserved by rename-only Tasks 1/3; dashboard reused (Task 4). ✅
- Plain Russian docs → Tasks 6 (UI), 9 (docs). ✅
- Only user-facing functionality on the main path, jargon hidden → Tasks 5, 9. ✅
- sing-box foundation only → Task 8. ✅
- Rebrand wg-split→splify → Tasks 1, 2. ✅
- Build apk + release + verify install works → Task 10. ✅

**Placeholder scan:** no TBD/TODO; code steps carry full code; doc steps specify concrete required content. ✅

**Type/name consistency:** ubus actions `on`/`off` (Task 3) match `home.js` calls (Task 5); `splify.dashboard` exports `buildPanel`/`injectStyle` (Task 4) match `home.js`/`status.js` consumers (Tasks 4/5); nft/env/path identifiers match Global Constraints; menu view paths `splify/home`,`splify/settings` match created view files. ✅
